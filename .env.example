OPENAI_API_KEY=your_openai_api_key_here

# Optional: set to 0 to silence the per-call OpenAI tracer that prints
# one line per chat-completion request to the server's terminal.
# Defaults to 1 (tracing ON).
PROMPTBREAKER_TRACE=1
